function Thing(id, name, url, public_url, thumbnail, creator)
{
    this.id            = id;
    this.name          = name;
    this.url           = url;
    this.public_url    = public_url;
    this.thumbnail     = thumbnail;
    this.creator       = creator;
}
