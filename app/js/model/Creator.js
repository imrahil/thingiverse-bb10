function Creator(id, name, full_name, url, public_url, thumbnail)
{
    this.id            = id;
    this.name          = name;
    this.full_name     = full_name;
    this.url           = url;
    this.public_url    = public_url;
    this.thumbnail     = thumbnail;
}
